# Return the first and the last letter of checkio in the word map.
def checkio(map):

    return None

#These "asserts" using only for self-checking and not necessary for auto-testing
if __name__ == '__main__':
    assert checkio(['IOPE', 'KCEE', 'EZHD', 'IACU']) == [[3, 2], [0, 1]]
    assert checkio(['OIKCEHC']) == [[6, 0], [0, 0]]
    print('Done')
